{
  "frontend/src/pages/LandingPage.tsx": "Landing page with parallax hero, features grid, sample invitation previews, CTA section, and footer — all auth/login logic removed; CTA buttons navigate directly to the dashboard.",
  "frontend/src/pages/NotFound.tsx": "Styled 404 page with floating heart icon, elegant gold/ivory typography, and navigation buttons back to home and dashboard.",
  "frontend/src/pages/Dashboard.tsx": "Creator dashboard with invitation card grid — all action buttons (Edit, Preview, Publish, Copy Link, RSVPs, Delete) are always rendered outside any conditional visibility logic, ensuring the Publish button is never hidden; also shows bride/groom photo thumbnails in card headers when available.",
  "frontend/src/pages/CreateInvitationWizard.tsx": "Multi-step invitation creation wizard — passes `submitError` prop to `ReviewStep` and uses `addPhotos` with `ExternalBlob` objects for couple photo uploads.",
  "frontend/src/pages/InvitationEditor.tsx": "Invitation editor with tabbed interface — fixed all type errors by using `EventData` instead of backend `Event` for state, using correct `EventForm` props (`initialEvent/onSave`), removing unsupported `TemplateThemeStep` props, and replacing `PhotoGalleryUpload`/`MusicUploader` with inline editor-specific components that accept explicit props.",
  "frontend/src/pages/GuestInvitation.tsx": "Guest-facing invitation page — passes correct props to all guest components: `invitationUrl/coupleName` to WhatsApp button, `string[]` photos to lightbox, and `invitation` only to RSVPForm.",
  "frontend/src/components/media/MusicUploader.tsx": "Music upload component with file picker, audio preview player with play/pause, auto-play toggle switch, and remove button — stores music as base64 data URL in form context.",
  "frontend/src/components/media/MusicController.tsx": "Refactored floating music controller — fixed mobile touch support with `touchAction: manipulation`, proper play() Promise handling for mobile autoplay restrictions, a separate expandable volume panel triggered by a Music2 icon button, minimum 44×44px touch targets, and a \"Tap ▶ for music\" hint shown before first user interaction.",
  "frontend/src/components/wizard/MediaManagementStep.tsx": "Step 4 of the wizard combining PhotoGalleryUpload and MusicUploader components in luxury card containers with icon headers.",
  "frontend/src/components/wizard/ReviewStep.tsx": "Review step component with optional `submitError` prop added to `ReviewStepProps`, displaying the error message inline above the action buttons.",
  "frontend/src/components/guest/HeroSection.tsx": "Full-screen hero section that renders bride and groom photos side by side in circular frames when available, using `ExternalBlob.getDirectURL()` for cached image URLs, with graceful fallback when photos are absent.",
  "frontend/src/components/guest/CountdownTimer.tsx": "Live countdown timer with dark/light mode detection, explicit digit and label colors (ivory on dark, charcoal on light), and semi-transparent digit cards with template-colored borders for readability across all templates.",
  "frontend/src/components/guest/CoupleIntroduction.tsx": "Scroll-animated couple introduction section that displays bride and groom photos side by side in styled cards using `ExternalBlob.getDirectURL()`, with a letter-avatar fallback when no photos are present.",
  "frontend/src/components/guest/EventTimeline.tsx": "Scroll-animated event cards with gradient headers using event-type-specific colors and explicit high-contrast text colors (white or near-black) on each gradient, plus a white card body with dark text for all detail rows.",
  "frontend/src/components/guest/PhotoGalleryLightbox.tsx": "Photo gallery lightbox — updated to accept `photos: string[]` (image URLs) and `invitation: Invitation` instead of `Photo[]` and `templateData`, resolving the type mismatch from `GuestInvitation`.",
  "frontend/src/components/guest/VenueSection.tsx": "Scroll-animated venue section with dark/light mode detection, explicit heading and body text colors, and a gold-gradient directions button with dark text for legibility across all templates.",
  "frontend/src/components/guest/RSVPForm.tsx": "Guest RSVP form component — removed the invalid `focusRingColor` CSS property from the guest name input's inline style object, keeping all other styling and form logic intact.",
  "frontend/src/components/guest/WhatsAppShareButton.tsx": "Fixed floating WhatsApp share button — updated props from `slug/brideName/groomName` to `invitationUrl/coupleName` to match how `GuestInvitation` calls it.",
  "frontend/src/components/SkeletonLoader.tsx": "Reusable skeleton loading component moved out of the read-only ui/ directory, supporting card/text/image/invitation-card variants with animated shimmer placeholders.",
  "frontend/src/hooks/useQueries.ts": "Comprehensive React Query hooks for all backend operations — removed `useEnsureUserRole` hook and all `useInternetIdentity`/`UserRole` imports so all queries and mutations work without authentication.",
  "frontend/src/components/dashboard/RSVPResponsesModal.tsx": "RSVP responses modal using correct hook names `useGetRSVPsByInvitation`/`useGetRSVPsStats`, with `invitationTitle` and `isOpen` made optional, and replaced Dialog with a custom overlay modal to remove prop requirement issues.",
  "frontend/src/components/layout/Header.tsx": "Navigation header that conditionally hides the \"Create Invitation\" CTA button when the current route is `/edit/*`, `/invitation/*`, or `/create`, preventing it from appearing on editor, wizard, and guest invitation pages.",
  "frontend/src/utils/templateDefinitions.ts": "Expanded template library with 12 total templates across 6 categories (Royal Indian, Modern Minimal, Cinematic Dark, Romantic, Boho & Floral, Vintage & Rustic) — 6 new templates added (Regal Emerald, Pastel Garden, Dusty Rose Romance, Midnight Luxe, Floral Boho, Rustic Parchment) with unique color palettes, font pairings, and `isNew` flags; also expanded COLOR_SCHEMES and FONT_CHOICES arrays.",
  "frontend/src/context/InvitationFormContext.tsx": "Form context for the invitation wizard — `updateEvent` now takes a full `EventData` object (matching how `EventManagementStep` calls it), with `LocalEvent` extending `EventData` with optional tracking flags.",
  "frontend/src/components/templates/TemplateSelector.tsx": "Redesigned template selector with 6 category groups, richer card previews using each template's actual colors/fonts, \"NEW\" badges on newly added templates, and improved visual hierarchy with category dividers.",
  "frontend/src/components/templates/TemplatePreview.tsx": "Live invitation preview component that reads all data (names, date, venue, template, color scheme, font, background) directly from InvitationFormContext and renders instantly without requiring a save action.",
  "frontend/src/components/wizard/TemplateThemeStep.tsx": "Template and theme customization step with live preview, section tabs (template, color, font, background, saved themes) — fixed `FontSelector` usage by passing the required `value` (from `formData.fontChoice`) and `onChange` (updating `fontChoice` via `updateFormData`) props.",
  "frontend/src/App.tsx": "Main app with TanStack Router, QueryClient, and ThemeProvider — defines all routes (home, dashboard, create, edit, invitation) without any AuthGuard wrappers, so all pages are freely accessible.",
  "frontend/src/components/auth/AuthGuard.tsx": "New auth guard component that checks Internet Identity login status and automatically assigns the `#user` role via `actor.assignCallerUserRole` when a user first authenticates, fixing the \"Unauthorized: Only users can create invitations\" error.",
  "frontend/src/index.css": "Global styles with OKLCH color tokens, extended Google Fonts imports including bold/premium fonts (Abril Fatface, Bodoni Moda, Cinzel Decorative, DM Serif Display, DM Serif Text, Cormorant SC, Bebas Neue, Oswald), shimmer/fadeInUp animations, and Tailwind directives.",
  "frontend/tailwind.config.js": "Tailwind config extending with gold/crimson/ivory/charcoal color palette, serif/sans font families, luxury box shadows, and shimmer/fadeInUp keyframe animations.",
  "frontend/src/components/templates/FontSelector.tsx": "Font pairing selector with 15 options grouped into Classic, Modern, Romantic, and Bold categories — the Bold category adds 8 new premium/impactful pairings (Playfair Black, Bodoni Moda, Abril Fatface, Cinzel Decorative, DM Serif Display, Cormorant SC, Bebas Neue, Oswald) with live sample text previews.",
  "frontend/src/components/wizard/CoupleDetailsStep.tsx": "Step 1 of the wizard with all couple detail fields plus two separate file upload controls (Bride Photo and Groom Photo), each with image preview thumbnail and remove button, wired to the updated form context."
}